bind up camup
bind down camdown
bind left camleft
bind right camright
bind a select
bind space select
bind s back
bind kp_9 ne
bind kp_1 sw
bind kp_2 down
bind kp_3 se
bind kp_4 left
bind kp_6 right
bind kp_7 nw
bind kp_8 up
bind axisn0 left
bind axisn3 left
bind axisp0 right
bind axisp3 right
bind axisn1 up
bind axisn4 up
bind axisp1 down
bind axisp4 down
bind joy1 select
bind joy2 back
bind mouse1 edit-paint
bind mouse4 edit-prev
bind mouse5 edit-next
bind pagedown edit-texdn
bind pageup edit-texup
bind kp_plus edit-laydn
bind kp_minus edit-layup
bind backspace edit-undo
